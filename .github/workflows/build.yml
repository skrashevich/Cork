name: CI   
                
on:
  push:
  workflow_dispatch:
                              
jobs:                        
  build:                     
    runs-on: macos-latest    
                              
    steps:                   
    - uses: actions/checkout@v2                                           
                              
    - name: Set Xcode version
      run: sudo xcode-select --switch /Applications/Xcode_14.2.app 
                              
                              
    - name: Build Cork App   
      run: |                 
        xcodebuild -scheme Cork -configuration Release -archivePath Cork.xcarchive archive         
        xcodebuild -exportArchive -archivePath Cork.xcarchive -exportPath CorkApp -exportOptionsPlist ExportOptions.plist                            
                              
    - name: Upload Artifact  
      uses: actions/upload-artifact@v2                                    
      with:                  
        name: Cork           
        path: CorkApp/Cork.app
        